spring:
  application:
    name: inventario
  datasource:
    url: jdbc:postgresql://localhost:5432/Lintick
    username: postgres
    password: mercad1a
    driver-class-name: org.postgresql.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    hibernate:
      ddl-auto: update
    show-sql: true
